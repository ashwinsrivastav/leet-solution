1class Solution:
2    def smallestRangeI(self, nums: List[int], k: int) -> int:
3        if abs(max(nums)-min(nums))<=k:
4            return 0
5        elif (max(nums)-k)-(min(nums)+k)<=0:
6            return 0
7        else:
8            return (max(nums)-k)-(min(nums)+k)