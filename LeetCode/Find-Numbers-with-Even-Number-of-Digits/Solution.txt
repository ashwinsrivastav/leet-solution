class Solution {
public:
    int findNumbers(vector<int>& nums) {
        int num=0;
        for(int i=0;i<nums.size();i++){
            int n = floor(log10(nums[i]))+1; 
            if(n%2==0)
            num++;
        }
        return num;
    }
};