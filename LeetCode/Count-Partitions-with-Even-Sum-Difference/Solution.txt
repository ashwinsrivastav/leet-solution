1class Solution:
2    def countPartitions(self, nums: List[int]) -> int:
3        count=0
4        for i in range(1,len(nums)):
5            a=sum(nums[:i])-sum(nums[i:])
6            if abs(a)%2==0:
7                count+=1
8        return count